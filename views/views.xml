<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="persebaya.list">
      <field name="name">persebaya list</field>
      <field name="model">persebaya.persebaya</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="persebaya.action_window">
      <field name="name">persebaya window</field>
      <field name="res_model">persebaya.persebaya</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="persebaya.action_server">
      <field name="name">persebaya server</field>
      <field name="model_id" ref="model_persebaya_persebaya"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="persebaya" id="persebaya.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="persebaya.menu_1" parent="persebaya.menu_root"/>
    <menuitem name="Menu 2" id="persebaya.menu_2" parent="persebaya.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="persebaya.menu_1_list" parent="persebaya.menu_1"
              action="persebaya.action_window"/>
    <menuitem name="Server to list" id="persebaya" parent="persebaya.menu_2"
              action="persebaya.action_server"/>
    -->
  </data>
</odoo>